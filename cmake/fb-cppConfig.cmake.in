@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Boost.DLL and Boost.Multiprecision are header-only - just need Boost headers
find_dependency(Boost)

# Firebird2 client library (transitive dependency)
# Look relative to this package's install location (sibling in ports tree)
find_dependency(Firebird2 CONFIG PATHS "${CMAKE_CURRENT_LIST_DIR}/../../../firebird2")

include("${CMAKE_CURRENT_LIST_DIR}/fb-cppTargets.cmake")

check_required_components(fb-cpp)
